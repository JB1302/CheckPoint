@model IEnumerable<CheckPoint.Models.Comments.Comment>

<div class="comments-list">
    @foreach (var comment in Model)
    {
        @await Html.PartialAsync("~/Views/Comments/_CommentItem.cshtml", comment)
    }
</div>
