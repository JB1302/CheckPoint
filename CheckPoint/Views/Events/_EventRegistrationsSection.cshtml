@model IEnumerable<CheckPoint.Models.Registrations.Registration>

<div class="card">
    <div class="card-header">
        <h5>Participantes (@Model.Count())</h5>
    </div>
    <div class="card-body">
        @await Html.PartialAsync("~/Views/Registrations/_RegistrationTable.cshtml", Model)
    </div>
</div>
